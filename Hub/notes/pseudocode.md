# Pseudocode

    # repeats until specified to end
    while is true do 
        # Makes the variables are integers given
        value 1 equals number(user input)
        value 2 equals number(user input)

        # repeat the function over n over
        function calculate()
            # adds the 2 valjues together in a total const int variable
            total equals value 1 + value 2
            output total
        end

        calculate() # runs
    end